import 'package:flutter/material.dart';

class PokemonListCell extends StatelessWidget {
  const PokemonListCell({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Espero que funcione da forma que eu achop que funciona"),
    );
  }
}
